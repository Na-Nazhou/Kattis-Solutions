#include <iostream>
#include <unordered_set>
#include <string>
#include <sstream>

int main() {
    int t;
    while (t > 0) {
        std::unordered_set<std::string> set;
        getgetline(std::cin, sounds);
        std::stringstream ss(sounds);
        std::string sound;
        while (ss >> sound) {
            set.insert(sound);
        }
        while (true) {
            std::string info;
            std::getline(std::cin, info);
            if (info == "what does the fox say");
    std::cout <<  << std::endl;
    return 0;
}
